# 🔄 请刷新浏览器

## 后端已更新完成

✅ 后端服务已重启
✅ 配置schema已更新为list类型
✅ 配置数据格式正确

## 需要刷新浏览器

前端显示的是缓存的旧数据，请执行以下操作：

### 方法1：硬刷新（推荐）

**Mac**: 
```
Cmd + Shift + R
```

**Windows/Linux**: 
```
Ctrl + Shift + R
```

### 方法2：清除缓存

1. 打开开发者工具（F12）
2. 右键点击刷新按钮
3. 选择"清空缓存并硬性重新加载"

### 方法3：禁用缓存

1. 打开开发者工具（F12）
2. 进入 Network 标签
3. 勾选 "Disable cache"
4. 刷新页面

## 刷新后的效果

配置界面将显示为人性化的表单：

```
┌─────────────────────────────────────────┐
│ 资源站 *                                 │
│ 添加多个资源站，支持所有海洋CMS兼容的API  │
├─────────────────────────────────────────┤
│ ┌─────────────────────────────────────┐ │
│ │ #1                              ✕   │ │
│ ├─────────────────────────────────────┤ │
│ │ 资源站名称                           │ │
│ │ [茅台资源                    ]       │ │
│ │                                     │ │
│ │ API地址                             │ │
│ │ [https://caiji.maotaizy.cc/...]    │ │
│ │                                     │ │
│ │ URL前缀（可选）                      │ │
│ │ [                            ]       │ │
│ │                                     │ │
│ │ URL后缀（可选，如$hym3u8）           │ │
│ │ [                            ]       │ │
│ │                                     │ │
│ │ ☑ 启用                              │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ #2                              ✕   │ │
│ ├─────────────────────────────────────┤ │
│ │ 资源站名称                           │ │
│ │ [虎牙资源                    ]       │ │
│ │                                     │ │
│ │ API地址                             │ │
│ │ [https://www.huyaapi.com/...]      │ │
│ │                                     │ │
│ │ URL前缀（可选）                      │ │
│ │ [                            ]       │ │
│ │                                     │ │
│ │ URL后缀（可选，如$hym3u8）           │ │
│ │ [$hym3u8                     ]       │ │
│ │                                     │ │
│ │ ☑ 启用                              │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ [+ 添加资源站]                          │
└─────────────────────────────────────────┘
```

## 验证

刷新后，在浏览器控制台执行以下命令验证：

```javascript
fetch('/api/plugins')
  .then(r => r.json())
  .then(data => {
    const seacms = data.search.find(p => p.name === 'seacms');
    console.log('✅ Type:', seacms.config_schema[0].type);
    console.log('✅ Label:', seacms.config_schema[0].label);
    console.log('✅ Fields:', seacms.config_schema[0].fields.length);
  });
```

**期望输出**:
```
✅ Type: list
✅ Label: 资源站
✅ Fields: 5
```

## 如果仍然显示JSON文本

1. 完全关闭浏览器
2. 重新打开浏览器
3. 访问设置页面

或者清除localStorage：

```javascript
localStorage.clear();
location.reload();
```

## 现在就试试吧！

按 `Cmd + Shift + R` (Mac) 或 `Ctrl + Shift + R` (Windows/Linux) 刷新页面！
